{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build && mkdir -p dist/scan dist/scan/host dist/demo && cp dist/index.html dist/scan/index.html && cp dist/index.html dist/scan/host/index.html && cp dist/index.html dist/demo/index.html",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/**/*.js": {
      "includeFiles": "server/**",
      "maxDuration": 30
    }
  }
}
